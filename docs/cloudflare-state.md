# Cloudflare State Report

This file is generated by `tools/sync-cloudflare-state.ps1`.

It is safe to commit because it should contain configuration names and command output only, not secret values.

Run from the repository root:

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\sync-cloudflare-state.ps1
```

Generated report will replace this placeholder.
